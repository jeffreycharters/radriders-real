from app.status.models import Status


def top_statuses(statuses, number=3):
    '''
    subscribed = set()
    for status in statuses:
        subscribed.add(status.trail_system)
    return subscribed
    '''
    return True
